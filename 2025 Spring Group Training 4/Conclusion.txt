URL:  https://codeforces.com/group/uMtJiIwg3J/contest/611801
FROM:   2023湖南省赛

A:签到 判断一个字符串是否可以拆成两个非空字符串拼接，第一个字符串不重复，第二个字符串是回文串。注意到字母只有26个，暴力即可。

B:ST表  将一个数组平分成若干块，问有多少个块大小可以保证：块内排序等价于全部排序

C:KMP  错误的随机数生成器会不断生成前缀相同的序列，KMP匹配前缀即可

E:dp 01?矩阵，可以将k个?变为1，后找一条左上右下最短路使得1的数量最多。暴力dp走到i,j,将x个?变1的最优即可

F:单调队列  给定n个数，从中选k个，最小化最大的差与最小的差乘积。排序后单调队列维护滑动窗口种最小差即可。

H:数学  将n拆成k个正实数之和，将每个实数四舍五入后再求和，求这个和的最大和最小。

K:构造 构造长度为n的序列使得相邻两项差的绝对值是质数，包括首项尾项之差。