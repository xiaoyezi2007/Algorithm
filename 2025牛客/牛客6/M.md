#### 题意：

给n个不同的数小于m。将其分配给两个数（填数位）。使得在m进制下两个数差最小。

#### 解：

先排序

偶数的话只需要找最靠近中间的两对差最小的数分别填到最高位，然后大数后面的位从小往大填，小数从大往小填。比较一下输出最优。

如果是奇数，如果最小不是0，那么长的那个数从最小开始依次往后，短的数从最大开始依次往前。

如果最小是0，那么两种情况：一种考虑长的数第一位填第二小的，一种考虑把0填给长的数，剩下情况类比偶数。